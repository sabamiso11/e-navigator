<h1>ようこそ</h1>
<p>トップページです。</p>
<% if @users.any? %>
    <table>
        <tr>
            <th>名前</th>
            <th>Email</th>
            <th>年齢</th>
            <th>性別</th>
            <th>学校名</th>
        </tr>
<% @users.each do |user| %>
        <tr>
            <td><%= user.username%></td>
            <td><%= user.email%></td>
            <td><%= (Date.today.strftime("%Y%m%d").to_i - user.birthday.strftime("%Y%m%d").to_i) /10000 %></td>
            <td><%= user.sex %></td>
            <td><%= user.company %></td>
        </tr>
<% end %>
    </table>
<% end %>